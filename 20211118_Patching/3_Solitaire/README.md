## Description
The goal of the challenge is to always have the same initial hand!

To install, open terminal in the folder and run

sudo apt-get install libncurses5-dev libncursesw5-dev

make

(make install)

## (not a) Solution
I'm too lazy to do this one (and I don't know how to play solitaire).
`ttysolitaire` is the already built binary but it probably won't work on your machine because it's not statically linked
